edit:
    - status-bar.ts
        - [ ] 将 StatusBar 中的 status 封装成枚举，remove 从枚举里通过 all 方法获取

---

add:
    - 配置
        - [ ] 添加 i18n 设置，可以设置中文或英文，默认使用中文
        - [ ] 使用 i18n key 替换项目中的文案，把 value 放到 lang 目录下

---

add:
    - ui
        - [ ] 在 Modifiers 区域上方添加一个横向滚动指示器：UI 为垂直滚动条的横向变体（仅保留滑块，隐藏轨道），支持手指拖拽左右滑动触发横向滚动，与 Modifiers 区域保持小间距

---

add:
    - 交互
        - [ ] 增加震动反馈